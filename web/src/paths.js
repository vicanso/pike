const prefix = "";

export const CACHES_PATH = `${prefix}/caches`;
export const COMPRESSES_PATH = `${prefix}/compresses`;
export const UPSTREAMS_PATH = `${prefix}/upstreams`;
export const LOCATIONS_PATH = `${prefix}/locations`;
